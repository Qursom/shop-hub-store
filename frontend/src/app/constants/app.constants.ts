/**
 * Global application constants.
 */
export const APP_CONSTANTS = {
  /**
   * Fallback image URL used when a product image fails to load or is not provided.
   */
  DEFAULT_PLACEHOLDER_URL: 'https://placehold.net/default.svg',

  /**
   * Milliseconds to wait after a successful order before redirecting to the products page.
   */
  ORDER_REDIRECT_DELAY_MS: 3000,
};
